#include "stdio.h"
#include "math.h"

#define EPS le-10

double l=5.0,u=10.0;

double func(double a){

return a*a
}


double algo(double a_1,double a_2_k,double p ){
  
  do{
    if(){


    }

    else if(){


    }
    else{



    }


  }while(<=EPS);

}
int main(){
  int k=1;
  double low[k]=l,up[k]=u;
  double p=(3-sqrt(5))/2;
  double a1[k]=low[k]+p(up[k]-low[k]);
  double a2[k]=up[k]-p(up[k]-low[k]);
  algo();
  double solution=(low[k]+up[k])/2;
  print("",);
  return 0;

}
